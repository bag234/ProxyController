spring:
  datasource:
    url: jdbc:h2:file:./testdb
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    show-sql: true
    hibernate:
      #ddl-auto: create #update table
      ddl-auto: update
  h2:
    console: 
      enabled: true 
      path: /h2
  application:
    name: ProxyOverlord
